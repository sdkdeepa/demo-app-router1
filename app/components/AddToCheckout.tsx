'use client'
import React from 'react'

const AddToCheckout = () => {
  return (
    <div>
      <button onClick={() => console.log('click')}>Add to checkout</button>
    </div>
  )
}

export default AddToCheckout
